# 智题 AIQuiz 项目评估报告（2026-08 复评）

> 评估日期：2026-08-21
> 评估方式：静态代码审查 + 关键指标实测（后端导入/pytest、前端 vue-tsc、git 状态、密钥与安全抽查）
> 与上一轮报告的关系：本报告是对《AIQuiz项目评估报告.md》（下称"上轮报告"）修复后的独立复评，验证其声称的修复是否落地，并重新盘点遗留与新发现问题。

---

## 一、项目概述：这是做什么的

**智题 AIQuiz** 是面向教育机构的 **AI 智能题库系统**，覆盖"出题 → 组卷 → 审核 → 考试"全链路：

| 模块 | 说明 |
|---|---|
| AI 出题 | 混合出题引擎：规则引擎定策略（知识点六类识别→题型占比→AI 提示词），AI 写题，规则模板兜底 —— **项目最大差异化亮点** |
| 智能组卷 | 参数化自动组卷、难度分布、A/B 卷、Word/PDF 导出 |
| 题库管理 | 题目 CRUD、Excel/Word 批量导入导出、相似度查重 |
| 试题审核 | 审核流 + 审核日志（含 IP/UA）+ 通知创建者 |
| 知识库 | 文档上传、AI/规则双通道知识点提取 |
| 考试管理 | 组卷发布、在线作答、客观题自动判分 |
| 权限体系 | 三角色 + 用户级菜单权限覆盖 |
| 其他 | 系统设置（密码策略/限流/AI 服务商管理）、试卷模板市场 |

**技术栈**：Python 3.11 / FastAPI 0.109 / SQLAlchemy 2.0 / PostgreSQL 15 / Redis 7 / Alembic；Vue 3.3 / Vite 5 / Element Plus / Pinia / TypeScript（渐进）；Docker Compose；OpenSpec 变更管理。

---

## 二、总体结论

| 维度 | 上轮 | 本轮 | 说明 |
|---|---|---|---|
| 可运行性 | ❌ 无法启动 | ✅ **正常** | 后端导入成功、pytest 56 passed、前端 typecheck 0 错误（均实测） |
| 安全性 | 3.0/5 | 3.5/5 | XSS/Key 泄露/IDOR/限流已封堵；但认证迁移半途而废、密钥未轮换 |
| 后端架构 | 3.0/5 | 3.5/5 | papers.py 拆分完成、AI Provider 收敛；knowledge.py 等仍超千行 |
| 前端架构 | 3.0/5 | 3.0/5 | composable 单例已修、死代码已清；但 8 个近千行巨型文件原封未动 |
| 工程化 | 2.5/5 | 2.5/5 | **版本控制失效是最大风险**：124 项变更未提交，CI 从未生效 |
| 测试 | 2.0/5 | 2.5/5 | 56 个测试全绿（含 P0 回归测试）；覆盖率相对 158 个 API 仍偏低 |

**综合评分：3.4 / 5 —— 已从"跑不起来"恢复到"可运行、有测试护栏"，但工程化短板（版本控制、CI）成为当前最大风险源。**

---

## 三、实测验证结果（本轮新证据）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 后端可启动 | ✅ | `import app.main` 成功（上轮 P0-1 tasks.py 语法错误已修复） |
| 后端测试 | ✅ | `pytest` **56 passed**（17s，含 P0 回归测试 ×7、安全回归 ×13） |
| 前端类型检查 | ✅ | `vue-tsc --noEmit` **0 错误**（上轮 160 错误已清零） |
| papers.py 拆分 | ✅ | 2300 → **736 行**（导出逻辑→paper_doc_exporter.py，组卷→papers_generate.py） |
| dashboard 假数据 | ✅ 已改真实数据源 | dashboard.py:260-330（题目/试卷/考试记录三源合并） |
| composable 模块级单例 | ✅ 已修复 | useAutoPaper.ts 等 4 文件模块顶层 `ref/reactive` 计数为 **0** |
| 前端死文件 | ✅ 已删除 | views/LoginView.vue stub、*.vue.bak、假 barrel 均不存在 |
| CVE 依赖 | ✅ 已升级 | requirements.txt：uvicorn 0.34 / python-multipart 0.0.26 / pypdf |
| httpOnly cookie（后端半） | ✅ | auth.py:117-133 登录种 cookie，get_current_user 优先读 cookie |
| AI Key 掩码 | ✅ | GET /settings 对密钥类设置掩码返回 |
| .env 是否进 git | ✅ 未跟踪 | 仅 .env.example 入库；历史记录中无密钥泄露 |

---

## 四、遗留与新发现问题清单

### 🔴 高优先级（P0 级）

#### H-1 版本控制基本失效：124 项变更未提交，工作区是唯一副本 ⛔ 本轮实测
- 全仓仅 3 个提交，最近一次距今约 100 天；工作区积压 **124 项未提交变更**。
- 未提交内容包括：**整个 `backend/alembic/`（数据库迁移！）**、`routers/system/`、`routers/questions/`、`ai_templates/`、`papers_generate.py`、`.github/workflows/ci.yml`、`Makefile`、`tests/`、前端全部 composables。
- 后果：① 任何磁盘/误操作事故 = 项目归零；② HEAD 指向的是"无法启动的旧快照"，clone 仓库得到的是坏版本；③ 数据库迁移不在版本库，他人无法重建库结构。
- 建议：立即按逻辑拆分提交并推送（先提交 alembic + 可运行基线）。此项上轮已建议，因用户要求暂缓——**风险随时间累积，强烈建议尽快执行**。

#### H-2 两个真实 MiniMax API Key 在本地明文存放且未轮换 ⛔ 本轮实测
- `backend/.env:6` 与 `deploy/.env:11` 各有一枚**不同的真实可用 Key**（`sk-cp-IyVa…` / `sk-cp-Vo6o…`）。
- 虽未进 git，但上轮评估会话中已被完整读取过（对话记录即泄露面），且两处并存无轮换记录。
- 建议：在 MiniMax 控制台轮换这两枚 Key；本地 .env 权限收紧；建立"密钥只进密钥管理器/部署平台"的规范。

#### H-3 前端认证迁移半途而废：仍在用 localStorage XOR "加密" token ⛔ 本轮实测
- 后端已完成 httpOnly cookie（auth.py:117-133），但前端 `api/index.js:18` 仍注入 `Authorization: Bearer ${authStore.token}`，token 存于 localStorage 且经硬编码密钥 XOR（stores/auth.js:5-7 `__secure_storage_key__`）；`withCredentials` 未开启。
- 后果：XSS 一旦发生即可窃取 token——上轮 P1-4 的风险实质未消除，只是后端具备了条件。
- 建议：前端登录后不再持有 token（删 XOR/localStorage 逻辑），axios 开 `withCredentials: true`，401 时调 `/refresh`；保留 Bearer 兼容期后移除。

### 🟠 中优先级（P1 级）

#### M-1 CI 从未生效且不完整 ⛔ 本轮实测
- `.github/workflows/ci.yml` 存在于工作区但**未提交**（远端仓库无 CI）。
- job 名 `backend-lint-and-test` 却**没有 ruff 步骤**（仓库已有 ruff.toml）；node-version "18" 已 EOL；未上传覆盖率产物。
- 建议：提交 ci.yml 并补 ruff check；node 升 20/22；加 `npm run typecheck` 到前端 job。

#### M-2 异步任务仍是裸 `threading.Thread`（daemon=True）
- tasks.py:304/374 两处。进程重启丢任务、状态永久卡 running、多副本部署互不可见、无重试。
- 建议：短期在启动时回收悬挂任务（扫描 running → 标记 failed）；中期引入 ARQ/RQ（已有 Redis 依赖，成本低）。

#### M-3 cookie `secure=False` 硬编码
- auth.py:131。生产 HTTPS 下 cookie 不带 Secure 标志，可被明文信道截获。
- 建议：改为 `secure=not settings.debug` 或独立配置项。

#### M-4 角色语义三方不一致（文档 vs 代码 vs 权限表）
- CLAUDE.md:101 仍写 "`student` (role=3)"，代码注释与提示语已是 "auditor/审核员"（security.py:104-110）。
- 注册流程已改为 role=3 + 零权限（合理），但文档未同步，新人理解成本高。
- 建议：产品层面定夺 role=3 语义后统一 CLAUDE.md、README、代码注释。

#### M-5 巨型文件债务：前端 8 个文件近千行或超千行 ⛔ 本轮实测
- PaperManagementView.vue **1540**、useAutoPaper.ts **1180**、AIQuestionView.vue 1088、useSystemSettings.ts 1013、AutoPaperView.vue 1010、UserPermissionView.vue 1007…
- 后端 knowledge.py **1283**、questions/import_export.py **1079** 亦超千行。
- 建议：按上轮路线图继续拆（前端拆子组件 + 按功能拆 composable；knowledge.py 拆 tree/crud/import）。

### 🟡 低优先级（P2 级）

| 编号 | 问题 | 位置/证据 |
|---|---|---|
| L-1 | `deploy/.env.example` 为 GBK 编码，UTF-8 工具打开乱码，复制出的 .env 继承编码问题 | 本轮实测：非 ASCII 字节非 UTF-8 序列 |
| L-2 | `MINIMAX_GROUP_ID` 配置残留，代码零引用 | backend/.env:7；rg 全仓 0 匹配 |
| L-3 | `cors_origins` 默认值硬编码 5 个 localhost 端口，生产依赖环境变量覆盖，有误配风险 | config.py:38-44 |
| L-4 | 测试覆盖偏低：56 测试 vs 158 API；papers/knowledge/audit 主链路仅回归测试覆盖 | tests/ 目录 |
| L-5 | `openspec/changes/archive/` 被 .gitignore 排除，11 个归档变更提案只在本地 | .gitignore:53 |
| L-6 | 技能文件三处重复（.claude/、.codebuddy/、backend/.claude/ 各 ~800 行） | 目录对比 |
| L-7 | JWT 无吊销机制（登出仅清 cookie，token 期内仍有效） | auth.py logout |

---

## 五、值得肯定的部分（应保持）

1. **混合出题引擎**（hybrid_question_generator.py）：规则做策略师、AI 做写手、模板兜底——真正的产品差异化。
2. **测试护栏从 0 到 56**：P0 回归测试 + 安全回归测试成体系，防止修复回退。
3. **统一响应封装 + 全局异常处理**：main.py 中间件链（安全头/CORS 收紧/envelope）质量良好。
4. **Alembic 迁移纪律**：main.py 不再 create_all，启动仅初始化默认设置。
5. **OpenSpec 变更管理真实在用**：3 个进行中 + 归档目录，需求可追溯。
6. **AI Provider 层收敛**：10 家 OpenAI 兼容厂商统一为 `_call_openai_compatible`，成本估算与调用日志齐备。

---

## 六、行动建议（按序执行）

| 优先级 | 行动 | 预估工作量 |
|---|---|---|
| 1 | **提交全部 124 项变更**（alembic 优先），推送双远端，恢复版本控制 | 0.5 天 |
| 2 | 轮换两枚 MiniMax Key；deploy/.env.example 转 UTF-8 | 0.5 小时 |
| 3 | 前端切换 httpOnly cookie 认证，删除 XOR/localStorage token 逻辑 | 1 天 |
| 4 | 提交 ci.yml + 补 ruff 步骤 + node 20；此后所有改动走 PR | 0.5 天 |
| 5 | 任务队列：启动回收悬挂任务（短期）→ ARQ（中期） | 1~3 天 |
| 6 | cookie secure 配置化；role=3 语义定夺并同步文档 | 0.5 天 |
| 7 | 巨型文件拆分（前端优先：PaperManagementView / useAutoPaper） | 1~2 周 |
| 8 | 补 papers/knowledge/audit 主链路集成测试，目标覆盖率 ≥50% | 1 周 |

---

## 七、附录：关键数据（本轮实测）

| 指标 | 数值 |
|---|---|
| 后端 pytest | **56 passed**（此前 0） |
| 前端 typecheck | **0 错误**（此前 160） |
| 后端最大文件 | knowledge.py 1283 行 / import_export.py 1079 行 |
| 前端最大文件 | PaperManagementView.vue 1540 行 / useAutoPaper.ts 1180 行 |
| 未提交变更 | **124 项**（含 alembic/、ci.yml、核心路由） |
| git 提交数 | 3（最近一次约 100 天前） |
| 真实密钥暴露 | 2 枚 MiniMax Key（backend/.env、deploy/.env，未轮换） |
| CI 状态 | 文件存在但未提交，从未生效 |

*本报告所有结论均通过直接运行验证（pytest、vue-tsc、git 检查、编码探测、grep 取证），无推测性断言。*
